services:
  - type: web
    name: angular-node-monorepo
    env: docker
    dockerContext: .
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: ACCESS_TOKEN_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false